$(document).ready(function() {

    // Let's add some nicer scrolling to the anchor tag of the portfolio section
   $('.container').localScroll({duration:1000});
  //  $('.home-hero.parallax--bg').localScroll({duration:1000});
});
